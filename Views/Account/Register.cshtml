@model RegisterViewModel
@{
    ViewData["Title"] = "Create Account";
}

<div class="page-header">
    <div class="breadcrumb">
        <a asp-controller="Home" asp-action="Index">Home</a><span>/</span><strong>Register</strong>
    </div>
    <h1 class="page-heading">Create account</h1>
</div>

<section class="surface-panel form-shell">
    <form asp-action="Register" method="post" class="stack-form">
        <input asp-for="ReturnUrl" type="hidden" />
        <div asp-validation-summary="ModelOnly" class="validation-summary"></div>

        <div class="form-grid">
            <label class="form-field">
                <span class="form-label">Display name</span>
                <input asp-for="DisplayName" class="form-input" autocomplete="name" />
                <span asp-validation-for="DisplayName" class="field-validation"></span>
            </label>
            <label class="form-field">
                <span class="form-label">Email</span>
                <input asp-for="Email" class="form-input" autocomplete="username" />
                <span asp-validation-for="Email" class="field-validation"></span>
            </label>
            <label class="form-field">
                <span class="form-label">OIB</span>
                <input asp-for="OIB" class="form-input" inputmode="numeric" autocomplete="off" />
                <span asp-validation-for="OIB" class="field-validation"></span>
            </label>
            <label class="form-field">
                <span class="form-label">JMBG</span>
                <input asp-for="JMBG" class="form-input" inputmode="numeric" autocomplete="off" />
                <span asp-validation-for="JMBG" class="field-validation"></span>
            </label>
            <label class="form-field">
                <span class="form-label">Password</span>
                <input asp-for="Password" class="form-input" autocomplete="new-password" />
                <span asp-validation-for="Password" class="field-validation"></span>
            </label>
            <label class="form-field">
                <span class="form-label">Confirm password</span>
                <input asp-for="ConfirmPassword" class="form-input" autocomplete="new-password" />
                <span asp-validation-for="ConfirmPassword" class="field-validation"></span>
            </label>
        </div>

        <div class="form-actions">
            <button type="submit" class="button-link button-link--solid">Create account</button>
            <a asp-action="Login" asp-route-returnUrl="@Model.ReturnUrl" class="button-link">Sign in</a>
        </div>
    </form>

    @if (Model.ExternalLogins.Count > 0)
    {
        <div class="auth-provider-strip">
            @foreach (var provider in Model.ExternalLogins)
            {
                var providerLabel = provider.DisplayName ?? provider.Name;
                var isGoogle = string.Equals(provider.Name, "Google", StringComparison.OrdinalIgnoreCase);
                var providerMark = isGoogle ? "G" : providerLabel.Substring(0, 1);
                <form asp-action="ExternalLogin" method="post" class="auth-provider-form">
                    <input type="hidden" name="provider" value="@provider.Name" />
                    <input type="hidden" name="returnUrl" value="@Model.ReturnUrl" />
                    <button type="submit" class="auth-provider-button @(isGoogle ? "auth-provider-button--google" : null)">
                        <span class="auth-provider-button__mark" aria-hidden="true">@providerMark</span>
                        <span>Continue with @providerLabel</span>
                    </button>
                </form>
            }
        </div>
    }
</section>
